import type { NextConfig } from 'next'

// Legacy URL redirects from the previous ASP.NET site live in src/middleware.ts,
// where matching is case-sensitive. See the note there for why.
const nextConfig: NextConfig = {}

export default nextConfig
